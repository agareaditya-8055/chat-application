import { MessageSquarePlus,Menu,Sparkles } from 'lucide-react'; import { Button } from '@/shared/components/ui/Button'; import { useUiStore } from '@/store/uiStore';
export function ChatEmptyState(){const open=useUiStore(s=>s.setSidebarOpen);return <section className="relative flex flex-1 items-center justify-center overflow-hidden bg-[hsl(var(--chat-background))] p-6"><Button className="absolute left-4 top-5 md:hidden" size="icon" variant="ghost" onClick={()=>open(true)}><Menu/></Button><div className="absolute inset-0 opacity-30 [background-image:radial-gradient(circle_at_center,hsl(var(--primary)/.16)_0,transparent_38%)]"/><div className="relative max-w-lg text-center"><div className="mx-auto mb-6 grid size-20 place-items-center rounded-[28px] border border-primary/20 bg-primary/10 text-primary shadow-soft"><Sparkles size={34}/></div><p className="mb-2 text-sm font-semibold text-primary">Your conversations, in one place</p><h1 className="text-3xl font-bold tracking-tight md:text-4xl">Turn messages into momentum.</h1><p className="mx-auto mt-4 max-w-md leading-7 text-muted-foreground">Choose a conversation from the sidebar, or start a new one and bring the right people together.</p><Button className="mt-7"><MessageSquarePlus size={18}/>Start a conversation</Button><div className="mt-12 flex justify-center gap-6 text-xs text-muted-foreground"><span>⌘ K Search</span><span>N New chat</span></div></div></section>}
