import { Component,type ErrorInfo,type ReactNode } from 'react'; import { Button } from './ui/Button';
export class ErrorBoundary extends Component<{children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}componentDidCatch(error:Error,info:ErrorInfo){console.error('Application boundary',error,info)}render(){if(this.state.failed)return <main className="grid min-h-screen place-items-center p-6"><div className="max-w-md text-center"><p className="mb-2 text-sm font-semibold text-primary">Something went wrong</p><h1 className="text-3xl font-bold">Relay lost the thread.</h1><p className="my-4 text-muted-foreground">Refresh the page to reconnect. Your messages are safe.</p><Button onClick={()=>location.reload()}>Refresh application</Button></div></main>;return this.props.children}}
