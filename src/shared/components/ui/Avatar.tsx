import * as AvatarPrimitive from '@radix-ui/react-avatar'; import { cn } from '@/shared/utils/cn';
export function Avatar({src,name,className}:{src?:string;name:string;className?:string}){return <AvatarPrimitive.Root className={cn('relative flex size-10 shrink-0 overflow-hidden rounded-xl bg-muted',className)}><AvatarPrimitive.Image src={src} alt={name} className="size-full object-cover"/><AvatarPrimitive.Fallback className="flex size-full items-center justify-center bg-gradient-to-br from-violet-100 to-indigo-100 text-xs font-bold text-indigo-700">{name.split(' ').map(v=>v[0]).slice(0,2).join('').toUpperCase()}</AvatarPrimitive.Fallback></AvatarPrimitive.Root>}
