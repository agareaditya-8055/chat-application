import { useCurrentUser } from '@/features/auth/hooks/useCurrentUser'; import { Avatar } from '@/shared/components/ui/Avatar'; import { Button } from '@/shared/components/ui/Button'; import { Input } from '@/shared/components/ui/Input'; export function ProfilePage(){const {data:user}=useCurrentUser();return <main className="min-h-screen bg-muted/40 p-6"><div className="mx-auto max-w-2xl rounded-3xl border border-border bg-background p-8 shadow-soft"><h1 className="text-2xl font-bold">Your profile</h1><p className="mt-1 text-muted-foreground">Manage how you appear across Relay.</p><div className="my-8 flex items-center gap-4"><Avatar name={user?.username??'User'} src={user?.avatarUrl} className="size-20 rounded-2xl"/><Button variant="outline">Upload new avatar</Button></div><div className="grid gap-5 sm:grid-cols-2"><label className="space-y-2 text-sm font-medium">Display name<Input defaultValue={user?.username}/></label><label className="space-y-2 text-sm font-medium">Email<Input defaultValue={user?.email} disabled/></label></div><Button className="mt-6">Save changes</Button></div></main>}
